# 九月影博客：外观与收藏维护说明

这份说明专门讲：头像、背景、顶部分类、左侧分类、个人收藏、本地图片/视频/歌曲如何添加和删除。

---

## 1. 更换头像

把头像图片放到：

```text
assets/images/avatar/
```

例如：

```text
assets/images/avatar/avatar.jpg
```

然后编辑：

```text
_data/site_settings.yml
```

把：

```yml
avatar: ""
```

改成：

```yml
avatar: "/assets/images/avatar/avatar.jpg"
```

保存后运行 `发布博客.bat`。

如果再次改回空字符串：

```yml
avatar: ""
```

网站会恢复默认的“九”圆形头像。

---

## 2. 更换页面背景

配置文件：

```text
_data/site_settings.yml
```

### 只改颜色

```yml
colors:
  page_background: "#f3f4f5"
  header_background: "#ffffff"
  left_menu_background: "#ffffff"
  content_background: "#ffffff"
  sidebar_background: "#ffffff"
```

### 使用自己的背景图

建议建立：

```text
assets/images/background/
```

例如：

```text
assets/images/background/site-bg.jpg
assets/images/background/header-bg.jpg
assets/images/background/left-bg.jpg
assets/images/background/sidebar-bg.jpg
```

配置：

```yml
backgrounds:
  page_image: "/assets/images/background/site-bg.jpg"
  header_image: "/assets/images/background/header-bg.jpg"
  left_menu_image: "/assets/images/background/left-bg.jpg"
  sidebar_image: "/assets/images/background/sidebar-bg.jpg"
```

不想用背景图时，把对应值改回：

```yml
page_image: ""
```

即可。

---

## 3. 顶部“项目 / 网址导航 / 歌曲分类”怎么增删

编辑：

```text
_data/top_nav.yml
```

当前：

```yml
- key: project
  label: 项目
  url: /

- key: nav
  label: 网址导航
  url: /nav/

- key: music
  label: 歌曲分类
  url: /music/
```

### 删除一个

例如不想显示“歌曲分类”，直接删除对应整段即可。

### 新增一个

例如新增“视频”：

```yml
- key: videos
  label: 视频
  url: /videos/
```

对应页面 `/videos/` 已经存在。

---

## 4. 左侧“项目”二级分类怎么增删

编辑：

```text
_data/project_categories.yml
```

每一个 `- key:` 开头的完整段落，就是一个左侧分类。

### 删除分类

删除该分类完整段落即可。

### 新增分类

例如：

```yml
- key: hadoop
  label: Hadoop
  slides:
    - title: Hadoop 学习笔记
      text: HDFS、MapReduce、YARN 等内容。
      image: "/assets/images/banner/hadoop.jpg"
      url: /hadoop/
```

对应文章写：

```yml
categories: [hadoop]
```

---

## 5. 右侧“个人收藏”怎么增删模块

现在“个人收藏”独立由：

```text
_data/collections.yml
```

控制。

目前只保留：

```text
图片收藏
视频收藏
```

因为顶部已经有“网址导航”和“歌曲分类”，所以不再在右侧重复显示。

### 新增模块

例如新增“游戏收藏”：

```yml
- key: games
  title: 游戏收藏
  description: 喜欢的游戏与攻略
  url: /games/
  icon: "🎮"
```

如果 `/games/` 页面还不存在，需要再创建页面。

### 删除模块

直接删除 `_data/collections.yml` 中对应完整段落。

---

## 6. 下载到本地的图片怎么放进去

推荐目录：

```text
assets/images/gallery/
```

例如：

```text
assets/images/gallery/genshin-01.jpg
```

然后编辑：

```text
_data/gallery.yml
```

添加：

```yml
- title: 原神收藏图 01
  image: /assets/images/gallery/genshin-01.jpg
  description: 我的图片收藏。
```

发布后会出现在 `/gallery/`。

---

## 7. 下载到本地的视频怎么放进去

推荐目录：

```text
assets/videos/
```

例如：

```text
assets/videos/huyao-01.mp4
```

封面图可以放：

```text
assets/images/video/huyao-01.jpg
```

编辑：

```text
_data/videos.yml
```

添加：

```yml
- title: 淮竹篇剪辑 01
  video: /assets/videos/huyao-01.mp4
  poster: /assets/images/video/huyao-01.jpg
  description: 本地视频收藏。
```

注意：GitHub 不适合存很多超大视频，较大的视频更建议用 B 站等外部平台嵌入。

---

## 8. 下载到本地的歌曲怎么放进去

推荐目录：

```text
assets/audio/
```

例如：

```text
assets/audio/zhiwo.mp3
```

编辑：

```text
_data/music.yml
```

写：

```yml
- title: 知我
  artist: 国风堂 / 哦漏
  category: 国风
  file: /assets/audio/zhiwo.mp3
  url: ""
```

网站歌曲页会直接显示 HTML5 播放器。

如果同时保留正版平台入口：

```yml
file: /assets/audio/zhiwo.mp3
url: "正版平台链接"
```

两者可以同时存在。

请只上传你有权公开托管的音频文件。

---

## 9. 最终发布

修改完任何配置或媒体文件后：

```text
1. 保存文件
2. 双击 发布博客.bat
3. 等 GitHub Actions 变绿
4. Ctrl + F5 刷新网站
```
