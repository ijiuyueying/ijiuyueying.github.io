# 九月影博客内容添加说明

这版网站采用“数据文件 + 页面自动读取”的方式维护。

## 1. 增加顶部一级分类

编辑 `_data/top_nav.yml`，复制一段：

```yml
- key: new
  label: 新分类
  url: /new/
```

然后新建 `new.md` 页面，并写入：

```md
---
layout: default
title: 新分类
permalink: /new/
nav_key: new
---

这里写页面内容。
```

## 2. 增加“项目”下面的二级分类

编辑 `_data/project_categories.yml`，新增：

```yml
- key: hadoop
  label: Hadoop
```

之后新建文章时，在文章顶部 YAML 中写：

```yml
categories: [hadoop]
```

首页左侧会自动出现 Hadoop 按钮；点击后会筛选出对应文章，并带淡入位移动画。

## 3. 给文章增加封面图

把图片放到 `assets/images/`，然后在文章 YAML 中增加：

```yml
cover: /assets/images/你的图片.jpg
```

首页轮播和文章卡片会自动读取封面图。

## 4. 增加网址导航

编辑 `_data/site_links.yml`：

```yml
- title: 网站名称
  url: https://example.com/
  description: 网站说明
  group: 开发工具
```

## 5. 增加歌曲

编辑 `_data/music.yml`：

```yml
- title: 歌曲名
  artist: 歌手
  category: 华语
  url: https://example.com/song
```

## 6. 增加图片收藏

先把图片放到 `assets/images/`，再编辑 `_data/gallery.yml`：

```yml
- title: 图片标题
  image: /assets/images/example.jpg
  description: 图片说明
```

## 7. 增加视频收藏

建议把较小的视频文件放到 `assets/videos/`，再编辑 `_data/videos.yml`：

```yml
- title: 视频标题
  video: /assets/videos/example.mp4
  poster: /assets/images/video-cover.jpg
  description: 视频说明
```

较大的视频不建议直接放 GitHub 仓库，可以填写外部可访问的视频地址。

## 8. 日常发布

继续使用原来的流程：Typora 写文章 → 保存到 `_posts` → 双击 `发布博客.bat`。
GitHub Actions 会自动构建并发布网站。
