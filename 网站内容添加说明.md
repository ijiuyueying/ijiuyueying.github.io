# 九月影博客内容添加说明

这版网站采用 **YAML 数据文件 + 页面自动读取** 的方式维护。日常不需要改 CSS / JavaScript，大部分内容只需要修改 `_data` 下的配置文件。

---

## 1. 日常同步和发布

### 先同步 GitHub 最新内容

在本地博客目录：

```text
E:\Blog\ijiuyueying.github.io
```

双击：

```text
同步博客.bat
```

### 写完内容后发布

Typora 写文章并保存后，双击：

```text
发布博客.bat
```

发布后 GitHub Actions 会自动构建网站。

---

## 2. 顶部一级分类怎么维护

顶部目前只显示：

```text
项目    网址导航    歌曲分类
```

配置文件：

```text
_data/top_nav.yml
```

当前结构类似：

```yml
- key: project
  label: 项目
  url: /

- key: nav
  label: 网址导航
  url: /nav/

- key: music
  label: 歌曲分类
  url: /music/
```

### 新增一个顶部一级分类

例如以后增加“视频”：

```yml
- key: video
  label: 视频
  url: /videos/
```

如果对应页面还不存在，需要同时创建相应页面。

> 建议：顶部一级分类不要加太多，保持 3～6 个比较合适。

---

## 3. “项目”下面的二级分类怎么维护

左侧这些：

```text
推荐
银行监管
制造业数仓
电商分析
SQL / Hive
Python
Git / GitHub
```

全部由这个文件控制：

```text
_data/project_categories.yml
```

每一个二级分类都可以同时配置：

- 分类名称
- 分类 key
- 对应轮播图
- 轮播标题
- 轮播说明
- 点击轮播图后跳转到哪里

### 示例：银行监管

```yml
- key: bank
  label: 银行监管
  slides:
    - title: 银行监管报送
      text: 1104 体系、监管指标、Mapping、数据清洗与数据治理。
      image: "/assets/images/banner/bank-01.jpg"
      url: /bank/

    - title: 监管数据治理
      text: 从业务数据到监管报表，梳理监管数据完整处理链路。
      image: "/assets/images/banner/bank-02.jpg"
      url: /bank/
```

### 新增 Hadoop 二级分类

在文件末尾追加：

```yml
- key: hadoop
  label: Hadoop
  slides:
    - title: Hadoop 学习笔记
      text: HDFS、MapReduce、YARN 等相关内容。
      image: "/assets/images/banner/hadoop-01.jpg"
      url: /hadoop/
```

然后 Hadoop 文章顶部写：

```yml
categories: [hadoop]
```

首页点击 Hadoop 后，会自动显示 Hadoop 的轮播和 Hadoop 文章。

---

## 4. 轮播图怎么更换

### 推荐方式：使用自己的本地图片

本地建立目录：

```text
E:\Blog\ijiuyueying.github.io\assets\images\banner
```

例如放入：

```text
bank-01.jpg
bank-02.jpg
manufacturing-01.jpg
ecommerce-01.jpg
sql-01.jpg
python-01.jpg
git-01.jpg
```

然后在：

```text
_data/project_categories.yml
```

修改：

```yml
image: "/assets/images/banner/bank-01.jpg"
```

保存后运行：

```text
发布博客.bat
```

### 使用网络图片

也可以直接写完整 URL：

```yml
image: "https://example.com/example.jpg"
```

但长期维护建议优先使用自己仓库里的图片，避免外部图片失效。

---

## 5. 一个分类放多张轮播图

在同一个 `slides:` 下面继续增加即可：

```yml
- key: ecommerce
  label: 电商分析
  slides:
    - title: 电商经营分析
      text: GMV、订单、用户、流量等经营分析。
      image: "/assets/images/banner/ecommerce-01.jpg"
      url: /ecommerce/

    - title: 商品和库存分析
      text: 动销率、售罄率、库存周转率等指标。
      image: "/assets/images/banner/ecommerce-02.jpg"
      url: /ecommerce/

    - title: 用户分析
      text: RFM、复购、新老客和用户分层。
      image: "/assets/images/banner/ecommerce-03.jpg"
      url: /ecommerce/
```

网站会自动：

- 每 5 秒轮播
- 显示左右箭头
- 显示底部圆点
- 点击圆点手动切换
- 点击左侧二级分类后换成该分类自己的轮播

---

## 6. 文章怎么归到对应项目分类

文章位于：

```text
_posts/
```

文章顶部 YAML 示例：

```yml
---
layout: post
title: "银行监管报送"
date: 2026-08-18 10:00:00 +0800
categories: [bank]
tags: [1104, G01, 数据治理]
---
```

关键是：

```yml
categories: [bank]
```

必须和 `_data/project_categories.yml` 中的：

```yml
key: bank
```

保持一致。

对应关系示例：

```text
银行监管      → bank
制造业数仓    → manufacturing
电商分析      → ecommerce
SQL / Hive   → sql-hive
Python       → python
Git / GitHub → git
```

---

## 7. 给单篇文章增加封面图

把图片放到：

```text
assets/images/
```

文章 YAML 增加：

```yml
cover: /assets/images/example.jpg
```

这个封面可以用于文章卡片等位置。

注意：**首页最上面的大轮播图现在主要由 `_data/project_categories.yml` 控制。**

---

## 8. 增加网址导航

编辑：

```text
_data/site_links.yml
```

增加：

```yml
- title: 网站名称
  url: https://example.com/
  description: 网站说明
  group: 开发工具
```

对应页面：

```text
/nav/
```

---

## 9. 增加歌曲

编辑：

```text
_data/music.yml
```

增加：

```yml
- title: 歌曲名
  artist: 歌手
  category: 华语
  url: https://example.com/song
```

对应页面：

```text
/music/
```

---

## 10. 增加图片收藏

先把图片放到：

```text
assets/images/gallery/
```

然后编辑：

```text
_data/gallery.yml
```

增加：

```yml
- title: 图片标题
  image: /assets/images/gallery/example.jpg
  description: 图片说明
```

对应页面：

```text
/gallery/
```

---

## 11. 增加视频收藏

建议小视频放：

```text
assets/videos/
```

封面图放：

```text
assets/images/video/
```

然后编辑：

```text
_data/videos.yml
```

增加：

```yml
- title: 视频标题
  video: /assets/videos/example.mp4
  poster: /assets/images/video/example.jpg
  description: 视频说明
```

对应页面：

```text
/videos/
```

较大的视频不建议直接存 GitHub 仓库，可以使用外部视频地址。

---

## 12. 最常用的维护流程

### 新写一篇银行监管文章

```text
新建文章
→ categories: [bank]
→ Typora 写内容
→ 保存
→ 发布博客.bat
```

### 换一张银行轮播图

```text
图片复制到 assets/images/banner/
→ 打开 _data/project_categories.yml
→ 修改 bank 对应 slide 的 image
→ 保存
→ 发布博客.bat
```

### 新增一个 Hadoop 项目分类

```text
_data/project_categories.yml 新增 hadoop
→ 配置 Hadoop 轮播图
→ Hadoop 文章写 categories: [hadoop]
→ 发布博客.bat
```

### 新增一个常用网址

```text
_data/site_links.yml
→ 新增一段网站配置
→ 发布博客.bat
```

---

## 13. 注意事项

1. YAML 缩进必须使用空格，不要使用 Tab。
2. `key` 建议全部使用英文小写，例如 `hadoop`、`spark`、`bank`。
3. 图片文件名建议使用英文和数字，例如 `bank-01.jpg`。
4. 一个文章原则上先归到一个主要项目分类，避免首页筛选逻辑混乱。
5. 修改任何 `_data/*.yml` 文件后，都要运行 `发布博客.bat` 才会上线。
6. GitHub Actions 变成绿色 ✅ 后，网站新内容才算发布成功。
