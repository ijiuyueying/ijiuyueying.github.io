# Typora + Git + GitHub Pages 本地使用说明

## 一、首次在电脑上配置（只做一次）

### 1. 把博客仓库克隆到电脑

在你准备存放博客的文件夹里打开 Git Bash 或终端，执行：

```bash
git clone https://github.com/ijiuyueying/ijiuyueying.github.io.git
```

克隆完成后，本地会出现文件夹：

```text
ijiuyueying.github.io
```

### 2. 用 Typora 打开整个博客文件夹

Typora 中文界面：

**文件 → 打开文件夹 → 选择 `ijiuyueying.github.io`**

以后尽量从这个文件夹里写博客，不要另外复制一份 Markdown 再发布。

### 3. Typora 图片设置

在 Typora：

**文件 → 偏好设置 → 图像**

插入图片时建议选择“复制到指定路径”，路径设置为：

```text
../assets/images/${filename}
```

同时建议勾选：

- 对本地位置的图片应用上述规则
- 优先使用相对路径

文章模板和自动新建文章脚本也会写入：

```yaml
typora-copy-images-to: ../assets/images/${filename}
```

## 二、最方便的新建文章方式

仓库根目录已经提供：

```text
新建博客文章.bat
```

双击后会依次询问：

1. 文章分类
2. 文章标题
3. 文件名

目前支持六个分类：

```text
1. bank            银行监管报送
2. manufacturing   制造业数仓
3. ecommerce       电商数据分析
4. sql-hive        SQL / Hive
5. python          Python
6. git             Git / GitHub
```

例如：

```text
分类：1
标题：G01资产负债项目统计表梳理
文件名：g01-overview
```

会自动生成：

```text
_posts/2026-08-18-g01-overview.md
```

并自动写入文章头信息，然后尝试使用 Typora 打开。

### 设置桌面快捷键

1. 在仓库中找到 `新建博客文章.bat`。
2. 右键 → **发送到 → 桌面快捷方式**。
3. 到桌面右键该快捷方式 → **属性**。
4. 找到 **快捷键**。
5. 按下例如 `Ctrl + Alt + N`。
6. 点击 **应用 → 确定**。

以后按 `Ctrl + Alt + N` 就能直接创建博客文章。

## 三、手动新建文章方式

如果不使用脚本，也可以直接在 `_posts` 里新建 Markdown。

文件名建议：

```text
YYYY-MM-DD-英文名称.md
```

例如：

```text
2026-08-18-g01-overview.md
```

文章最顶部至少保留：

```yaml
---
layout: post
title: "文章标题"
date: 2026-08-18 11:00:00 +0800
categories: [bank]
tags: []
typora-copy-images-to: ../assets/images/${filename}
---
```

## 四、写完以后发布

保存 Typora 后，直接双击：

```text
发布博客.bat
```

它会自动执行：

```text
git add → git commit → git pull --rebase → git push
```

推送成功后，GitHub Pages 会自动重新发布网站。

网站地址：

<https://ijiuyueying.github.io/>

## 五、换电脑继续写

如果新电脑从未下载过仓库，先重新执行 `git clone`。

如果这台电脑已经有博客仓库，写之前先双击：

```text
同步博客.bat
```

同步完成后再用 Typora 编辑，可以减少多台电脑之间的版本冲突。

## 六、常用原则

- 首页和栏目页已经配置好，日常写文章不要再去 GitHub 网页里创建栏目。
- 日常文章统一放 `_posts`。
- 图片统一放 `assets/images`。
- `_drafts/文章模板.md` 是备用模板，不是必须使用。
- 新建文章优先使用 `新建博客文章.bat`。
- 如果 `发布博客.bat` 显示冲突，不要随便删除文件，保留窗口并排查冲突。
