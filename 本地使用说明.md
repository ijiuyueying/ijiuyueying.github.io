# Typora + Git + GitHub Pages 本地使用说明

## 一、首次在电脑上配置（只做一次）

### 1. 把博客仓库克隆到电脑

在你准备存放博客的文件夹里打开 Git Bash 或终端，执行：

```bash
git clone https://github.com/ijiuyueying/ijiuyueying.github.io.git
```

克隆完成后，本地会出现文件夹：

```text
ijiuyueying.github.io
```

### 2. 用 Typora 打开整个博客文件夹

Typora 中文界面：

**文件 → 打开文件夹 → 选择 `ijiuyueying.github.io`**

以后尽量从这个文件夹里写博客，不要另外复制一份 Markdown 再发布。

### 3. Typora 图片设置

在 Typora：

**文件 → 偏好设置 → 图像**

建议开启“插入本地图片时复制到指定文件夹/允许复制图片到指定文件夹”。文章模板已经设置：

```yaml
typora-copy-images-to: ../assets/images/${filename}
```

这样从 `_posts` 里的文章粘贴图片时，可以把图片集中放到：

```text
assets/images/文章文件名/
```

## 二、以后写一篇新文章

### 1. 复制文章模板

模板位置：

```text
_drafts/文章模板.md
```

复制到：

```text
_posts/
```

文件名必须使用：

```text
YYYY-MM-DD-英文名称.md
```

例如：

```text
2026-08-18-g01-overview.md
```

文章标题可以使用中文，不要求文件名也写中文。

### 2. 设置文章分类

可使用以下分类：

```text
bank
manufacturing
ecommerce
sql-hive
python
git
```

例如银行监管文章：

```yaml
categories: [bank]
```

发布后会自动出现在“银行监管报送”栏目，不需要再手动修改栏目页面。

## 三、写完以后发布

保存 Typora 后，直接双击：

```text
发布博客.bat
```

它会自动执行：

```text
git add → git commit → git pull --rebase → git push
```

推送成功后，GitHub Pages 会自动重新发布网站。

网站地址：

<https://ijiuyueying.github.io/>

## 四、换电脑继续写

如果新电脑从未下载过仓库，先重新执行 `git clone`。

如果这台电脑已经有博客仓库，写之前先双击：

```text
同步博客.bat
```

同步完成后再用 Typora 编辑，可以减少多台电脑之间的版本冲突。

## 五、常用原则

- 首页和栏目页已经配置好，日常写文章不要再去 GitHub 网页里创建栏目。
- 日常文章统一放 `_posts`。
- 图片统一放 `assets/images`。
- `_drafts/文章模板.md` 是模板，不会正常发布成博客文章。
- 如果 `发布博客.bat` 显示冲突，不要随便删除文件，保留窗口并排查冲突。
