# 九月影博客：分类层级与模块说明

## 一、整站固定结构

网站统一采用：

```text
一级分类（顶部）
→ 二级分类（当前模块左侧）
→ 三级分类（左侧缩进子项）
→ 中间内容
→ 右侧公共栏
```

右侧公共栏会在项目、网址导航、歌曲、图片、视频模块中持续显示。

---

## 二、一级分类

配置文件：

```text
_data/top_nav.yml
```

当前：

```text
项目 / 网址导航 / 歌曲分类
```

以后要把“图片收藏”或“视频收藏”提升为顶部一级分类，也是在这里新增。

---

## 三、项目模块二级 / 三级分类

配置：

```text
_data/project_categories.yml
```

示例：

```yml
- key: bank
  label: 银行监管
  children:
    - key: 1104
      label: 1104体系
    - key: reports
      label: G01 / G11 / G12
    - key: governance
      label: 数据治理
```

文章属于二级分类：

```yml
categories: [bank]
```

文章属于三级分类：

```yml
categories: [bank]
subcategory: governance
```

例如：

```yml
---
layout: post
title: "监管数据治理案例"
categories: [bank]
subcategory: governance
---
```

---

## 四、网址 / 歌曲 / 图片 / 视频的二三级分类

配置文件：

```text
_data/module_menus.yml
```

当前示例：

```text
网址导航
├─ 开发工具
│  └─ 代码托管
└─ AI
   └─ 大模型

歌曲分类
└─ 国风
   └─ 动画音乐

图片收藏
└─ 游戏
   └─ 原神

视频收藏
└─ 动漫
   ├─ 狐妖小红娘
   └─ 淮竹篇
```

实际内容对应字段统一使用：

```yml
group: 二级分类key
subgroup: 三级分类key
```

例如图片：

```yml
- title: 原神壁纸
  image: /assets/images/gallery/genshin.jpg
  group: game
  subgroup: genshin
```

例如视频：

```yml
- title: 淮竹篇剪辑
  bvid: BVxxxx
  group: anime
  subgroup: huaizhu
```

例如网址：

```yml
- title: GitHub
  url: https://github.com/
  group: dev
  subgroup: code-hosting
```

---

## 五、右侧公共栏

文件：

```text
_includes/global-rightbar.html
```

右侧会根据当前模块自动变化：

```text
项目 → 好文推荐
网址导航 → 常用网址
歌曲分类 → 最近歌曲
图片收藏 → 图片推荐
视频收藏 → 视频推荐
```

下面的“个人收藏”统一读取：

```text
_data/collections.yml
```

因此以后增加收藏模块，只需要在 `collections.yml` 增加一段配置。

---

## 六、搜索框

顶部搜索框是全局固定位置，但搜索范围跟随当前模块：

```text
项目页 → 搜索项目文章
网址导航 → 搜索网站名称和说明
歌曲 → 搜索歌曲、歌手、分类
图片 → 搜索图片标题和说明
视频 → 搜索视频标题和说明
```

---

## 七、推荐的层级原则

不要为了“有三级分类”强行拆分。

建议：

```text
一级：大模块
二级：稳定主题
三级：具体知识方向 / 内容类型
```

例如：

```text
项目
└─ 银行监管
   ├─ 1104体系
   ├─ 监管报表
   └─ 数据治理
```

三级分类再往下不建议继续做第四级；如果内容更细，使用文章标签 `tags` 即可。
